--- a/TelnyxRTC/Telnyx/TxClient.swift
+++ b/TelnyxRTC/Telnyx/TxClient.swift
@@ -296,6 +296,7 @@
     /// }
     /// ```
     public func enableAudioSession(audioSession: AVAudioSession) {
+        guard audioDevice == nil else { return }
         setupCorrectAudioConfiguration()
         setAudioSessionActive(true)
     }
@@ -315,6 +316,7 @@
     /// }
     /// ```
     public func disableAudioSession(audioSession: AVAudioSession) {
+        guard audioDevice == nil else { return }
         resetAudioConfiguration()
         setAudioSessionActive(false)
     }
@@ -332,7 +334,11 @@
 
     // MARK: - Initializers
     /// TxClient has to be instantiated.
-    public init() {
+    private let audioDevice: RTCAudioDevice?
+
+    /// Ember POC: a custom device owns PCM and does not use AVAudioSession.
+    public init(audioDevice: RTCAudioDevice? = nil) {
+        self.audioDevice = audioDevice
         self.serverConfiguration = TxServerConfiguration()
         self.configure()
         sessionId = UUID().uuidString.lowercased()
@@ -441,6 +447,7 @@
     ///
     /// @objc attribute is required for NotificationCenter selector
     @objc private func handleAudioRouteChange(notification: Notification) {
+        guard audioDevice == nil else { return }
         guard let userInfo = notification.userInfo,
               let reasonValue = userInfo[AVAudioSessionRouteChangeReasonKey] as? UInt,
               let reason = AVAudioSession.RouteChangeReason(rawValue: reasonValue) else {
@@ -1326,7 +1333,7 @@
             throw TxError.callFailed(reason: .destinationNumberIsRequired)
         }
 
-        let call = Call(callId: callId,
+        let call = Call(callId: callId, audioDevice: audioDevice,
                         remoteSdp: "",
                         sessionId: sessionId,
                         socket: socket,
@@ -1437,7 +1444,7 @@
         let forceRelayCandidate = (self.txConfig?.forceRelayCandidate ?? false)
             || (isAttach && takeForceRelayForNextRecoveredCall())
 
-        let call = Call(callId: appFacingCallId,
+        let call = Call(callId: appFacingCallId, audioDevice: audioDevice,
                         signalingCallId: signalingCallId,
                         remoteSdp: remoteSdp,
                         sessionId: sessionId,
@@ -1560,7 +1567,7 @@
                    let callUUID = UUID(uuidString: newCallId),
                    let socket = self.socket,
                    let iceServers = self.storedServerConfiguration?.webRTCIceServers {
-                    self.calls[callUUID] = Call(callId: callUUID,
+                    self.calls[callUUID] = Call(callId: callUUID, audioDevice: audioDevice,
                                                remoteSdp: "",
                                                sessionId: newCallId,
                                                socket: socket,
@@ -1606,6 +1613,7 @@
 
     /// Select the internal earpiece as the audio output
     public func setEarpiece() {
+        guard audioDevice == nil else { return }
         Logger.log.i(message: "[ACM_RESET] TxClient:: setEarpiece() called")
         do {
             let audioSession = AVAudioSession.sharedInstance()
@@ -1619,6 +1627,7 @@
 
     /// Select the speaker as the audio output
     public func setSpeaker() {
+        guard audioDevice == nil else { return }
         Logger.log.i(message: "[ACM_RESET] TxClient:: setSpeaker() called")
         do {
             let audioSession = AVAudioSession.sharedInstance()
@@ -2212,6 +2221,7 @@
 // MARK: - Audio session configurations
 extension TxClient {
     internal func resetAudioConfiguration() {
+        guard audioDevice == nil else { return }
         let audioSession = AVAudioSession.sharedInstance()
         do {
             try audioSession.setCategory(
@@ -2225,6 +2235,7 @@
     }
 
     internal func setupCorrectAudioConfiguration() {
+        guard audioDevice == nil else { return }
         let rtcAudioSession = RTCAudioSession.sharedInstance()
         rtcAudioSession.lockForConfiguration()
         
@@ -2244,6 +2255,7 @@
     }
 
     internal func setAudioSessionActive(_ active: Bool) {
+        guard audioDevice == nil else { return }
         let rtcAudioSession = RTCAudioSession.sharedInstance()
         
         rtcAudioSession.lockForConfiguration()
--- a/TelnyxRTC/Telnyx/WebRTC/Call.swift
+++ b/TelnyxRTC/Telnyx/WebRTC/Call.swift
@@ -422,7 +422,10 @@
 
     // MARK: - Initializers
     /// Constructor for incoming calls
+    private let audioDevice: RTCAudioDevice?
+
     init(callId: UUID,
+         audioDevice: RTCAudioDevice? = nil,
          signalingCallId: UUID? = nil,
          remoteSdp: String,
          sessionId: String,
@@ -447,6 +450,7 @@
          pushWhenActive: Bool = false,
          pushDeviceToken: String? = nil
     ) {
+        self.audioDevice = audioDevice
         if isAttach {
             self.direction = CallDirection.ATTACH
         } else {
@@ -499,6 +503,7 @@
     
     //Contructor for attachCalls
     init(callId: UUID,
+         audioDevice: RTCAudioDevice? = nil,
          signalingCallId: UUID? = nil,
          remoteSdp: String,
          sessionId: String,
@@ -518,6 +523,7 @@
          callReportMaxLogEntries: Int = 1000,
          pushWhenActive: Bool = false,
          pushDeviceToken: String? = nil) {
+        self.audioDevice = audioDevice
         self.direction = CallDirection.ATTACH
         //Session obtained after login with the signaling socket
         self.sessionId = sessionId
@@ -553,6 +559,7 @@
 
     /// Constructor for outgoing calls
     init(callId: UUID,
+         audioDevice: RTCAudioDevice? = nil,
          sessionId: String,
          socket: Socket,
          delegate: CallProtocol,
@@ -569,6 +576,7 @@
          callReportMaxLogEntries: Int = 1000,
          pushWhenActive: Bool = false,
          pushDeviceToken: String? = nil) {
+        self.audioDevice = audioDevice
         //Session obtained after login with the signaling socket
         self.sessionId = sessionId
         //this is the signaling server socket
@@ -625,7 +633,7 @@
         // - Start the reporter once the peer connection is created
         self.configureStatsReporter()
         Logger.log.i(message: "[TRICKLE-ICE] Call:: Creating Peer for outbound call with useTrickleIce = \(self.useTrickleIce)")
-        self.peer = Peer(iceServers: self.iceServers, forceRelayCandidate: self.forceRelayCandidate, useTrickleIce: self.useTrickleIce, isAnswering: false)
+        self.peer = Peer(iceServers: self.iceServers, audioDevice: audioDevice, forceRelayCandidate: self.forceRelayCandidate, useTrickleIce: self.useTrickleIce, isAnswering: false)
         self.startStatsReporter()
         self.peer?.delegate = self
         self.peer?.socket = self.socket
@@ -912,7 +920,7 @@
         self.answerCustomHeaders = customHeaders
         self.configureStatsReporter()
         Logger.log.i(message: "[TRICKLE-ICE] Call:: Creating Peer for inbound call answer with useTrickleIce = \(self.useTrickleIce)")
-        self.peer = Peer(iceServers: self.iceServers, forceRelayCandidate: self.forceRelayCandidate, useTrickleIce: self.useTrickleIce, isAnswering: true)
+        self.peer = Peer(iceServers: self.iceServers, audioDevice: audioDevice, forceRelayCandidate: self.forceRelayCandidate, useTrickleIce: self.useTrickleIce, isAnswering: true)
         self.enableQualityMetrics = debug
         self.startStatsReporter()
         self.peer?.delegate = self
@@ -959,7 +967,7 @@
         self.answerCustomHeaders = customHeaders
         self.configureStatsReporter(reportID: reportId)
         Logger.log.i(message: "[TRICKLE-ICE] Call:: Creating Peer for ATTACH with useTrickleIce = \(self.useTrickleIce)")
-        self.peer = Peer(iceServers: self.iceServers,
+        self.peer = Peer(iceServers: self.iceServers, audioDevice: audioDevice,
                          isAttach: true,
                          forceRelayCandidate: self.forceRelayCandidate,
                          useTrickleIce: self.useTrickleIce,
@@ -1745,6 +1753,7 @@
     }
 
     private func buildAudioPlayer(fileName: String?,fileType:SoundFileType) -> AVAudioPlayer? {
+        guard audioDevice == nil else { return nil }
         guard let file = fileName,
               let path = Bundle.main.path(forResource: file, ofType: nil ) else {
             Logger.log.w(message: "Call:: buildAudioPlayer() file not found: \(fileName ?? "Unknown").")
--- a/TelnyxRTC/Telnyx/WebRTC/Peer.swift
+++ b/TelnyxRTC/Telnyx/WebRTC/Peer.swift
@@ -197,16 +197,30 @@
     }()
 
 
+    // Ember: custom peers own a factory, leaving ordinary calls unchanged.
+    private let mediaFactory: RTCPeerConnectionFactory
+    private let usesCustomAudioDevice: Bool
+
     @available(*, unavailable)
     override init() {
         fatalError("Peer:init is unavailable")
     }
 
     required init(iceServers: [RTCIceServer],
+                  audioDevice: RTCAudioDevice? = nil,
                   isAttach: Bool = false,
                   forceRelayCandidate: Bool = false,
                   useTrickleIce: Bool = false,
                   isAnswering: Bool = false) {
+        self.usesCustomAudioDevice = audioDevice != nil
+        if let audioDevice {
+            RTCInitializeSSL()
+            self.mediaFactory = RTCPeerConnectionFactory(
+                encoderFactory: RTCDefaultVideoEncoderFactory(),
+                decoderFactory: RTCDefaultVideoDecoderFactory(), audioDevice: audioDevice)
+        } else {
+            self.mediaFactory = Peer.factory
+        }
         self.configuredIceServers = iceServers
         self.isAnswering = isAnswering
 
@@ -227,7 +241,7 @@
 
         let constraints = RTCMediaConstraints(mandatoryConstraints: nil,
                                               optionalConstraints: ["DtlsSrtpKeyAgreement": kRTCMediaConstraintsValueTrue])
-        self.connection = Peer.factory.peerConnection(with: config, constraints: constraints, delegate: nil)
+        self.connection = mediaFactory.peerConnection(with: config, constraints: constraints, delegate: nil)
 
         super.init()
         self.useTrickleIce = useTrickleIce
@@ -244,7 +258,7 @@
         let streamId = UUID.init().uuidString.lowercased()
 
         // Create local media stream
-        self._localStream = Peer.factory.mediaStream(withStreamId: streamId)
+        self._localStream = mediaFactory.mediaStream(withStreamId: streamId)
 
         // let's support Audio first.
         let audioTrack = self.createAudioTrack()
@@ -271,6 +285,7 @@
     ///
     /// This method runs asynchronously on a dedicated audio queue to prevent blocking.
     internal func configureAudioSession() {
+        guard !usesCustomAudioDevice else { return }
         self.audioQueue.async { [weak self] in
             guard let self = self else {
                 return
@@ -298,15 +313,17 @@
     }
 
     private func createAudioTrack() -> RTCAudioTrack {
-        let audioConstrains = RTCMediaConstraints(mandatoryConstraints: nil, optionalConstraints: nil)
-        let audioSource = Peer.factory.audioSource(with: audioConstrains)
+        let audioConstrains = RTCMediaConstraints(mandatoryConstraints: nil, optionalConstraints:
+            usesCustomAudioDevice ? ["googEchoCancellation": "false", "googAutoGainControl": "false",
+                                    "googNoiseSuppression": "false", "googHighpassFilter": "false"] : nil)
+        let audioSource = mediaFactory.audioSource(with: audioConstrains)
         //TODO: trackId should be auto generated.
-        let audioTrack = Peer.factory.audioTrack(with: audioSource, trackId: AUDIO_TRACK_ID)
+        let audioTrack = mediaFactory.audioTrack(with: audioSource, trackId: AUDIO_TRACK_ID)
         return audioTrack
     }
 
     private func createVideoTrack() -> RTCVideoTrack {
-        let videoSource = Peer.factory.videoSource()
+        let videoSource = mediaFactory.videoSource()
         
         #if targetEnvironment(simulator)
             //if we are using the simulator:
@@ -317,7 +334,7 @@
             self.videoCapturer = RTCCameraVideoCapturer(delegate: videoSource)
         #endif
         //TODO: trackId should be auto generated.
-        let videoTrack = Peer.factory.videoTrack(with: videoSource, trackId: VIDEO_TRACK_ID)
+        let videoTrack = mediaFactory.videoTrack(with: videoSource, trackId: VIDEO_TRACK_ID)
         return videoTrack
     }
 
@@ -350,7 +367,7 @@
         }
 
         // Get all supported audio codec capabilities from the factory
-        let capabilities = Peer.factory.rtpSenderCapabilities(forKind: kRTCMediaStreamTrackKindAudio)
+        let capabilities = mediaFactory.rtpSenderCapabilities(forKind: kRTCMediaStreamTrackKindAudio)
         let allCodecs = capabilities.codecs
 
         // Match and order the codecs according to user preferences
@@ -705,6 +722,7 @@
     /// - Parameter preserveSpeakerState: Whether to preserve the current speakerphone state during reset
     /// - Parameter forceSpeakerState: Optional forced speaker state to use instead of detecting current state
     func resetAudioDeviceModule(preserveSpeakerState: Bool = true, forceSpeakerState: Bool? = nil) {
+        guard !usesCustomAudioDevice else { return }
         guard let connection = self.connection else {
             Logger.log.w(message: "[ACM_RESET] Peer:: resetAudioDeviceModule() - No active connection")
             return
